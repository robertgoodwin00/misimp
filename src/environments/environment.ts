// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.


//var domain = "https://2607:f2d8:4010:2c::1ae1:ee75:3000";
var domain = 'http://localhost:4200';
//var domain = 'https://www.chattyfiction.cyou:2096';



export const environment = {
  production: false,
  //apiUrl: domain + '/frontend/app',
  //socketUrl: domain + '/chat',
  //faqUrl: domain + '/faq/getlista',
  //apiUrl: 'http://datingo2.us-3.evennode.com/frontend/app'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
